public enum TokenID {
    // Keywords
    AS, IN, NEIGHBORHOODS, NOT, OTHERWISE, RULES, STATES, STAY, VISUAL, WORLD,

    // Literals
    NUMBER, RANGE, IDENTIFIER,

    // Non-Keyword Operators
    APPLICATION,

    // Whitespace
    NEWLINE, INDENT, SPACE,

    EOF
}
